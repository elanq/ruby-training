//iterate documents
-@docs.each do |doc|
   %h2= link_to doc.title, doc
   //format timestamp by last created date
   %p= time_ago_in_words(doc.created_at)
   //truncate document content to 50 char
   %p= truncate(doc.content, length:50)

= link_to "Create new doc", new_doc_path