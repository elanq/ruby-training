- @messages.each do | msg |
  .col-md-10.col-md-offset-1
    .message
      .message-title
        %h2= msg.title
      = link_to "Join conversation", msg, class: "btn-custom"

=link_to "Post message", new_message_path    