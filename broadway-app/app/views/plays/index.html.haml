.index
  %h1.current-category
    - if params[:category]
      = params[:category]  
    - else
      Playlist
  - if @plays.count == 0
    %h3 There are no plays in this category
  - else   
    - @plays.each do |play|  
      .col-md-3
        - ref = "/plays/"+play.id.to_s
        %a{:href => ref}
          = image_tag play.play_img.url(:play_index), class: 'play'  