.col-md-8.col-md-offset-2
  %h1.current-category
    - if params[:category]
      = params[:category]  
    - else
      Playlist
  - if @plays.count == 0
    %h3 There are no plays in this category
  - else   
    - @plays.each do |play|  
      %h2= link_to "["+play.category.name+"] "+play.title, play_path(play)